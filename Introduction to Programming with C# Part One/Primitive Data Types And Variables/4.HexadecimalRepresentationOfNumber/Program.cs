using System;

namespace _4.HexadecimalRepresentationOfNumber
{
    class Program
    {
        static void Main()
        {
            int hexadecimalNumber = 0xFE;
            Console.WriteLine("The number FE in hexadecimal format is the number {0} in decimal format.", hexadecimalNumber);
        }
    }
}
